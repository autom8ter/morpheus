query ($id: String, $type: String!, $key: String!) {
    add(add: {
        id: $id
        type: $type,
        properties: {
            "name": "coleman"
        }
    }){
        properties
    }
}